import {LoginForm} from "@/components/modules/admin";

export default function LoginPage() {
  return (
      <section className="flex min-h-screen items-center justify-center">
        <LoginForm />
      </section>
  );
}